---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: vex-os-cosmic
# description will be included in the image's metadata
description: This is my personal OS image.

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/blue-build/base-images/fedora-cosmic
image-version: latest # latest is also supported if you want new updates ASAP

# module configuration, executed in order
# you can include multiple instances of the same module
modules:

  - from-file: module-recipes/core/files.yml
  - from-file: module-recipes/core/scripts.yml
  - from-file: module-recipes/cosmic/dnf.yml
  - from-file: module-recipes/core/code.yml
  - from-file: module-recipes/core/default-flatpaks.yml
  - from-file: module-recipes/core/just.yml
  - from-file: module-recipes/core/wallpapers.yml
  - from-file: module-recipes/core/bling.yml
  
  - type: os-release
    properties:
      ID: vex-os-cosmic
      ID_LIKE: fedora
      NAME: VexOS Cosmic
      PRETTY_NAME: VexOS Cosmic
      HOME_URL: "https://github.com/VictoryTek/vex-os"
      BUG_REPORT_URL: "https://github.com/VictoryTek/vex-os/issues"
      DEFAULT_HOSTNAME: vex-os-cosmic

  - type: initramfs
  - type: signing # this sets up the proper policy & signing files for signed images to work fully